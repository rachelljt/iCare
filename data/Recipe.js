class Recipe {
  constructor(title, imageURL, steps, ingredients) {
    this.title = title;
    this.imageURL = imageURL;
    this.steps = steps;
    this.ingredients = ingredients;
  }
}

export default Recipe;
